<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.movie.repository.MovieMapper">
    <!--  게시글 저장  -->
    <insert id="saveId">
        insert into movie_id(
			id,
			title,
			overview
        ) values (
			#{id},
			#{title},
			#{overview}
        )
    </insert>
    	
</mapper>